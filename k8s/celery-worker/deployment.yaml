# Celery Worker Deployment
# Processes async tasks from Redis queue
apiVersion: apps/v1
kind: Deployment
metadata:
  name: celery-worker
  namespace: chatplatform
  labels:
    app.kubernetes.io/name: chatplatform
    app.kubernetes.io/component: celery-worker
spec:
  # Pinned to 1 replica because celery-worker shares the media-pvc
  # PersistentVolumeClaim with the backend deployment so it can read
  # uploaded import files. The PVC is ReadWriteOnce (K3s local-path),
  # so only one celery-worker pod can attach the volume — N>1 leaves
  # N-1 pods empty and breaks imports.
  replicas: 1
  selector:
    matchLabels:
      app: celery-worker
  # RollingUpdate — safe now that media-pvc uses ReadWriteOncePod.
  # maxUnavailable:0 ensures at least one worker is always running.
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: celery-worker
        app.kubernetes.io/name: chatplatform
        app.kubernetes.io/component: celery-worker
    spec:
      # Mount the same media-pvc the backend writes to, so the import
      # task can read the .xlsx file the user uploaded.
      volumes:
        - name: media-files
          persistentVolumeClaim:
            claimName: media-pvc
      containers:
        - name: celery-worker
          image: nemo2092/chatplatform-backend:f782d54
          imagePullPolicy: IfNotPresent
          command:
            - celery
            - -A
            - config
            - worker
            - -l
            - info
            - --concurrency=2
          envFrom:
            - configMapRef:
                name: chatplatform-config
            - secretRef:
                name: chatplatform-secrets
          env:
            - name: DATABASE_URL
              value: "postgres://$(POSTGRES_USER):$(POSTGRES_PASSWORD)@postgres:5432/$(POSTGRES_DB)"
          # Mount the shared media volume so this worker can read
          # uploaded import files written by the backend pod.
          volumeMounts:
            - name: media-files
              mountPath: /app/media
          resources:
            requests:
              memory: "512Mi"
              cpu: "100m"
            limits:
              memory: "1Gi"
              cpu: "500m"

          # Liveness probe using celery inspect
          livenessProbe:
            exec:
              command:
                - celery
                - -A
                - config
                - inspect
                - ping
                - --timeout
                - "10"
            initialDelaySeconds: 60
            periodSeconds: 60
            timeoutSeconds: 30
            failureThreshold: 3

          # Graceful shutdown: tell celery to finish current tasks then exit.
          # The || true prevents FailedPreStopHook warnings if the broker
          # is momentarily unreachable during the rolling transition.
          lifecycle:
            preStop:
              exec:
                command:
                  - /bin/sh
                  - -c
                  - "celery -A config control shutdown 2>/dev/null || true; sleep 5"

      # Allow 5 minutes for graceful shutdown (long-running tasks)
      terminationGracePeriodSeconds: 300
