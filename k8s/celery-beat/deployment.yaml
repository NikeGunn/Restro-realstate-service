# Celery Beat Deployment
# Schedules periodic tasks - MUST be single instance
apiVersion: apps/v1
kind: Deployment
metadata:
  name: celery-beat
  namespace: chatplatform
  labels:
    app.kubernetes.io/name: chatplatform
    app.kubernetes.io/component: celery-beat
spec:
  replicas: 1  # CRITICAL: Must be exactly 1 to avoid duplicate scheduling
  selector:
    matchLabels:
      app: celery-beat
  strategy:
    type: Recreate  # Not RollingUpdate - only one instance allowed
  template:
    metadata:
      labels:
        app: celery-beat
        app.kubernetes.io/name: chatplatform
        app.kubernetes.io/component: celery-beat
    spec:
      containers:
        - name: celery-beat
          image: nemo2092/chatplatform-backend:5d1e588
          imagePullPolicy: IfNotPresent
          command:
            - celery
            - -A
            - config
            - beat
            - -l
            - info
            - --scheduler
            - django_celery_beat.schedulers.DatabaseScheduler
          envFrom:
            - configMapRef:
                name: chatplatform-config
            - secretRef:
                name: chatplatform-secrets
          env:
            - name: DATABASE_URL
              value: "postgres://$(POSTGRES_USER):$(POSTGRES_PASSWORD)@postgres:5432/$(POSTGRES_DB)"
          resources:
            requests:
              memory: "256Mi"
              cpu: "50m"
            limits:
              memory: "512Mi"
              cpu: "200m"
          livenessProbe:
            exec:
              command:
                - /bin/sh
                - -c
                - "celery -A config inspect ping --timeout 10 2>/dev/null | grep -q pong"
            initialDelaySeconds: 60
            periodSeconds: 60
            timeoutSeconds: 20
            failureThreshold: 3

      terminationGracePeriodSeconds: 60
